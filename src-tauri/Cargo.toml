[package]
name = "Cue"
version = "0.1.0"
description = "A desktop music controller for spotifyd"
authors = ["rice"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.3", features = [] }

[dependencies]
tauri = { version = "2.0.6", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
mpris = "2.1.0"
